to create headnode:

1) copy exampleVM.xml to [yourVM].xml
2) edit the xml file to:
    - change the vm name to your name
    - change UUID (might be necessary?)
    - set the ram and disk space required
    - modify the disk image path to match your image
3) run virsh define [yourVM].xml
4) virsh start [yourVM]
